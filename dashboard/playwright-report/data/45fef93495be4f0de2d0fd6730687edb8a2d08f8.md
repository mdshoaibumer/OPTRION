# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: topology.spec.ts >> Topology Page >> renders page title and description
- Location: e2e\topology.spec.ts:8:7

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('h1')
Expected substring: "Infrastructure Topology"
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toContainText" with timeout 5000ms
  - waiting for locator('h1')

```

```yaml
- text: "{\"success\":false,\"error\":{\"code\":\"ERROR\",\"message\":\"Cannot GET /topology\"}}"
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | 
  3  | test.describe("Topology Page", () => {
  4  |   test.beforeEach(async ({ page }) => {
  5  |     await page.goto("/topology");
  6  |   });
  7  | 
  8  |   test("renders page title and description", async ({ page }) => {
> 9  |     await expect(page.locator("h1")).toContainText("Infrastructure Topology");
     |                                      ^ Error: expect(locator).toContainText(expected) failed
  10 |     await expect(
  11 |       page.locator("text=Component dependency graph")
  12 |     ).toBeVisible();
  13 |   });
  14 | 
  15 |   test("displays topology nodes", async ({ page }) => {
  16 |     await expect(page.locator("text=Backend API")).toBeVisible();
  17 |     await expect(page.locator("text=Auth Service")).toBeVisible();
  18 |     await expect(page.locator("text=PostgreSQL Primary")).toBeVisible();
  19 |     await expect(page.locator("text=Redis Cache")).toBeVisible();
  20 |     await expect(page.locator("text=NGINX Proxy")).toBeVisible();
  21 |   });
  22 | 
  23 |   test("topology nodes are interactive (clickable)", async ({ page }) => {
  24 |     const node = page.locator("text=Backend API");
  25 |     await expect(node).toBeVisible();
  26 |     await node.click();
  27 |     // Node should still be visible after click (selection state)
  28 |     await expect(node).toBeVisible();
  29 |   });
  30 | });
  31 | 
```