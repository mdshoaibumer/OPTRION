# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: incidents.spec.ts >> Incidents Page >> renders page title
- Location: e2e\incidents.spec.ts:8:7

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('h1')
Expected substring: "Incident War Room"
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toContainText" with timeout 5000ms
  - waiting for locator('h1')

```

```yaml
- text: "{\"success\":false,\"error\":{\"code\":\"ERROR\",\"message\":\"Cannot GET /incidents\"}}"
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | 
  3  | test.describe("Incidents Page", () => {
  4  |   test.beforeEach(async ({ page }) => {
  5  |     await page.goto("/incidents");
  6  |   });
  7  | 
  8  |   test("renders page title", async ({ page }) => {
> 9  |     await expect(page.locator("h1")).toContainText("Incident War Room");
     |                                      ^ Error: expect(locator).toContainText(expected) failed
  10 |     await expect(
  11 |       page.locator("text=Manage active incidents")
  12 |     ).toBeVisible();
  13 |   });
  14 | 
  15 |   test("displays incident list", async ({ page }) => {
  16 |     await expect(
  17 |       page.locator("text=PostgreSQL connection pool exhausted")
  18 |     ).toBeVisible();
  19 |     await expect(
  20 |       page.locator("text=Redis cache hit ratio below threshold")
  21 |     ).toBeVisible();
  22 |     await expect(
  23 |       page.locator("text=API response latency spike")
  24 |     ).toBeVisible();
  25 |   });
  26 | 
  27 |   test("can select an incident to view details", async ({ page }) => {
  28 |     await page.locator("text=PostgreSQL connection pool exhausted").click();
  29 |     // Detail panel should show the full description
  30 |     await expect(
  31 |       page.locator("text=Connection pool utilization exceeded 95%")
  32 |     ).toBeVisible();
  33 |   });
  34 | });
  35 | 
```