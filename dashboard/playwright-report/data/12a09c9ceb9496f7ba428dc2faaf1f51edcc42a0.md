# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: health.spec.ts >> Health Page >> displays component health cards
- Location: e2e\health.spec.ts:12:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('text=Backend API')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('text=Backend API')

```

```yaml
- text: "{\"success\":false,\"error\":{\"code\":\"ERROR\",\"message\":\"Cannot GET /health\"}}"
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | 
  3  | test.describe("Health Page", () => {
  4  |   test.beforeEach(async ({ page }) => {
  5  |     await page.goto("/health");
  6  |   });
  7  | 
  8  |   test("renders page title", async ({ page }) => {
  9  |     await expect(page.locator("h1")).toContainText("Component Health");
  10 |   });
  11 | 
  12 |   test("displays component health cards", async ({ page }) => {
> 13 |     await expect(page.locator("text=Backend API")).toBeVisible();
     |                                                    ^ Error: expect(locator).toBeVisible() failed
  14 |     await expect(page.locator("text=PostgreSQL Primary")).toBeVisible();
  15 |     await expect(page.locator("text=Redis Cache")).toBeVisible();
  16 |     await expect(page.locator("text=Redis Sessions")).toBeVisible();
  17 |     await expect(page.locator("text=NGINX Proxy")).toBeVisible();
  18 |   });
  19 | 
  20 |   test("displays health metrics for components", async ({ page }) => {
  21 |     await expect(page.locator("text=Latency P99")).toBeVisible();
  22 |     await expect(page.locator("text=Error Rate")).toBeVisible();
  23 |     await expect(page.locator("text=Connections")).toBeVisible();
  24 |   });
  25 | 
  26 |   test("shows health scores", async ({ page }) => {
  27 |     // Backend API score 95
  28 |     await expect(page.locator("text=95")).toBeVisible();
  29 |     // PostgreSQL degraded score 62
  30 |     await expect(page.locator("text=62")).toBeVisible();
  31 |   });
  32 | });
  33 | 
```